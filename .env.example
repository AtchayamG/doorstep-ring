# Ring Playground tokens are never saved in an env file.
# Run ops/with-ring-token.ps1 to enter a 30-minute token for one process only.

# AWS Configuration (default uses ~/.aws/credentials and us-east-1)
AWS_REGION=us-east-1
BEDROCK_MODEL_ID=amazon.nova-pro-v1:0

# Service Port
PORT=3002
